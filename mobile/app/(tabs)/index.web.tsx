/**
 * Map screen — web. Interactive map via OpenStreetMap embed; same regions as native (Queensland, Sumatra, Hat Yai).
 */
import React from 'react';
import { WebMapView } from '../../components/WebMapView';

export default function MapScreenWeb() {
  return <WebMapView />;
}
